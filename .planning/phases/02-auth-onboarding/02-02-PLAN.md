---
phase: 02-auth-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hearth-budget/src/lib/schemas/auth.ts
  - hearth-budget/src/lib/schemas/onboarding.ts
  - hearth-budget/src/components/ui/input.tsx
  - hearth-budget/src/components/ui/label.tsx
  - hearth-budget/src/components/ui/form.tsx
  - hearth-budget/src/components/ui/tabs.tsx
  - hearth-budget/src/components/ui/radio-group.tsx
  - hearth-budget/src/components/ui/sonner.tsx
  - hearth-budget/src/components/ui/separator.tsx
  - hearth-budget/src/components/ui/avatar.tsx
  - hearth-budget/src/components/ui/sheet.tsx
  - hearth-budget/src/components/ui/sidebar.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "All required shadcn components are installed and importable"
    - "Zod v4 schemas exist for auth forms and onboarding steps"
    - "Schemas use Zod v4 API: z.email() top-level, error: '...' not message: '...'"
  artifacts:
    - path: "hearth-budget/src/lib/schemas/auth.ts"
      provides: "Zod v4 schemas for signup, login, magic link forms"
      exports: ["signUpSchema", "magicLinkSchema", "SignUpInput", "MagicLinkInput"]
    - path: "hearth-budget/src/lib/schemas/onboarding.ts"
      provides: "Zod v4 schemas for 3-step onboarding form"
      exports: ["householdNameSchema", "locationSchema", "incomeBracketSchema", "INCOME_BRACKETS"]
    - path: "hearth-budget/src/components/ui/form.tsx"
      provides: "shadcn Form component for react-hook-form integration"
  key_links:
    - from: "hearth-budget/src/lib/schemas/auth.ts"
      to: "react-hook-form via @hookform/resolvers/zod"
      via: "zodResolver(signUpSchema) in auth form components"
      pattern: "z\\.email\\(\\|z\\.string\\(\\).*min"
    - from: "hearth-budget/src/lib/schemas/onboarding.ts"
      to: "onboarding page multi-step form"
      via: "zodResolver(householdNameSchema) per step"
      pattern: "INCOME_BRACKETS"
---

<objective>
Install the required shadcn/ui components for Phase 2 UI and define all Zod v4 validation schemas for auth forms and onboarding steps.

Purpose: Auth pages (Plan 03) and onboarding (Plan 04) both import from these schemas and components. Defining contracts here prevents duplicate definitions and ensures consistent Zod v4 API usage throughout the phase.

Output: All shadcn UI components installed into src/components/ui/; two schema files exporting typed Zod schemas and form input types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-bootstrap/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Zod v4 API — DO NOT use v3 syntax -->
<!-- Source: hearth-budget/node_modules/zod/README.md + Next.js 16 auth guide -->

Zod v4 breaking changes from v3:
- `z.string().email()` is REMOVED → use `z.email()` as top-level primitive
- Error messages use `{ error: '...' }` NOT `{ message: '...' }` (v3 style)
- `z.enum()` accepts array of string literals directly

Correct Zod v4 patterns:
```typescript
import * as z from 'zod'

// CORRECT v4
const emailField = z.email({ error: 'Please enter a valid email address.' }).trim()
const nameField = z.string().min(1, { error: 'Name is required.' }).trim()

// WRONG — v3 style, do not use
const emailWrong = z.string().email({ message: 'Invalid email' })  // compile error in v4
```

shadcn components.json (from Phase 1):
- style: "new-york" variant (Nova preset)
- rsc: true (React Server Components enabled)
- tsx: true
- tailwind.css: "src/app/globals.css"
- aliases.components: "@/components"
- aliases.ui: "@/components/ui"
- aliases.utils: "@/lib/utils"

Run shadcn commands from hearth-budget/ with pnpm in PATH:
```bash
export PNPM_HOME="$HOME/Library/pnpm" && PATH="$PNPM_HOME:$PATH"
cd /Users/waylansmac/budget/hearth-budget
npx shadcn@latest add <component>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn UI components</name>
  <files>
    hearth-budget/src/components/ui/input.tsx,
    hearth-budget/src/components/ui/label.tsx,
    hearth-budget/src/components/ui/form.tsx,
    hearth-budget/src/components/ui/tabs.tsx,
    hearth-budget/src/components/ui/radio-group.tsx,
    hearth-budget/src/components/ui/sonner.tsx,
    hearth-budget/src/components/ui/separator.tsx,
    hearth-budget/src/components/ui/avatar.tsx,
    hearth-budget/src/components/ui/sheet.tsx,
    hearth-budget/src/components/ui/sidebar.tsx
  </files>
  <read_first>
    - hearth-budget/components.json (shadcn config — confirm style and paths)
    - hearth-budget/src/components/ui/ (list what already exists from Phase 1)
  </read_first>
  <action>
    Run the shadcn add command to install all required components at once.
    The -y flag accepts defaults without prompting.

    ```bash
    export PNPM_HOME="$HOME/Library/pnpm" && PATH="$PNPM_HOME:$PATH"
    cd /Users/waylansmac/budget/hearth-budget
    npx shadcn@latest add input label form tabs radio-group sonner separator avatar sheet sidebar -y
    ```

    This copies component source files into src/components/ui/. Each file is owned by
    the project (not a runtime dependency). If any component already exists (button.tsx,
    card.tsx from Phase 1), shadcn will ask to overwrite — accept with y for all.

    After installation, run pnpm build to confirm no TypeScript errors from new components:
    ```bash
    export PNPM_HOME="$HOME/Library/pnpm" && PATH="$PNPM_HOME:$PATH"
    cd /Users/waylansmac/budget/hearth-budget
    pnpm build
    ```

    Note: The sidebar component from shadcn v4 requires the sidebar CSS tokens that were
    already added to globals.css by shadcn init (--sidebar, --sidebar-foreground, etc.).
    No manual CSS changes needed.
  </action>
  <verify>
    <automated>
      ls /Users/waylansmac/budget/hearth-budget/src/components/ui/form.tsx /Users/waylansmac/budget/hearth-budget/src/components/ui/sonner.tsx /Users/waylansmac/budget/hearth-budget/src/components/ui/sidebar.tsx /Users/waylansmac/budget/hearth-budget/src/components/ui/tabs.tsx /Users/waylansmac/budget/hearth-budget/src/components/ui/radio-group.tsx 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - All 10 component files exist in hearth-budget/src/components/ui/:
      input.tsx, label.tsx, form.tsx, tabs.tsx, radio-group.tsx, sonner.tsx,
      separator.tsx, avatar.tsx, sheet.tsx, sidebar.tsx
    - grep "FormField\|FormItem\|FormLabel\|FormMessage" hearth-budget/src/components/ui/form.tsx returns matches
    - grep "Toaster" hearth-budget/src/components/ui/sonner.tsx returns a match
    - grep "SidebarProvider\|SidebarContent" hearth-budget/src/components/ui/sidebar.tsx returns matches
    - pnpm build from hearth-budget/ exits 0
  </acceptance_criteria>
  <done>All shadcn components installed; build passes with no TypeScript errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Zod v4 schemas for auth and onboarding forms</name>
  <files>
    hearth-budget/src/lib/schemas/auth.ts,
    hearth-budget/src/lib/schemas/onboarding.ts
  </files>
  <read_first>
    - hearth-budget/package.json (verify zod@4.3.6 installed)
    - hearth-budget/src/lib/utils.ts (confirm @/lib path alias works)
  </read_first>
  <behavior>
    auth.ts schemas:
    - signUpSchema: email (z.email) + password (min 8 chars) — both trimmed
    - signInSchema: email (z.email) + password (min 1) — both trimmed
    - magicLinkSchema: email (z.email) — trimmed
    - All use { error: '...' } syntax (Zod v4)
    - Export inferred TypeScript types: SignUpInput, SignInInput, MagicLinkInput

    onboarding.ts schemas:
    - householdNameSchema: name (string, min 1, max 100, trimmed)
    - locationSchema: zip (regex /^\d{5}$/) + metro (string, min 1, trimmed)
    - incomeBracketSchema: income_bracket (z.enum of 8 BLS brackets)
    - INCOME_BRACKETS: readonly string[] const for use in RadioGroup UI
    - Export inferred types: HouseholdNameInput, LocationInput, IncomeBracketInput
    - Export INCOME_BRACKETS constant (used in onboarding Step 3 UI)
  </behavior>
  <action>
    Create hearth-budget/src/lib/schemas/auth.ts:

    ```typescript
    import * as z from 'zod'

    export const signUpSchema = z.object({
      email: z.email({ error: 'Please enter a valid email address.' }).trim(),
      password: z
        .string()
        .min(8, { error: 'Password must be at least 8 characters.' })
        .trim(),
    })

    export const signInSchema = z.object({
      email: z.email({ error: 'Please enter a valid email address.' }).trim(),
      password: z.string().min(1, { error: 'Password is required.' }).trim(),
    })

    export const magicLinkSchema = z.object({
      email: z.email({ error: 'Please enter a valid email address.' }).trim(),
    })

    export type SignUpInput = z.infer<typeof signUpSchema>
    export type SignInInput = z.infer<typeof signInSchema>
    export type MagicLinkInput = z.infer<typeof magicLinkSchema>
    ```

    Create hearth-budget/src/lib/schemas/onboarding.ts:

    ```typescript
    import * as z from 'zod'

    export const INCOME_BRACKETS = [
      '<$15k',
      '$15-30k',
      '$30-40k',
      '$40-50k',
      '$50-70k',
      '$70-100k',
      '$100-150k',
      '$150k+',
    ] as const

    export type IncomeBracket = (typeof INCOME_BRACKETS)[number]

    export const householdNameSchema = z.object({
      name: z
        .string()
        .min(1, { error: 'Household name is required.' })
        .max(100, { error: 'Household name must be under 100 characters.' })
        .trim(),
    })

    export const locationSchema = z.object({
      zip: z
        .string()
        .regex(/^\d{5}$/, { error: 'Enter a valid 5-digit ZIP code.' }),
      metro: z.string().min(1, { error: 'Metro area is required.' }).trim(),
    })

    export const incomeBracketSchema = z.object({
      income_bracket: z.enum(INCOME_BRACKETS, {
        error: 'Please select an income bracket.',
      }),
    })

    export type HouseholdNameInput = z.infer<typeof householdNameSchema>
    export type LocationInput = z.infer<typeof locationSchema>
    export type IncomeBracketInput = z.infer<typeof incomeBracketSchema>
    ```

    Then verify TypeScript compilation:
    ```bash
    export PNPM_HOME="$HOME/Library/pnpm" && PATH="$PNPM_HOME:$PATH"
    cd /Users/waylansmac/budget/hearth-budget
    pnpm tsc --noEmit
    ```
  </action>
  <verify>
    <automated>
      cd /Users/waylansmac/budget/hearth-budget && export PNPM_HOME="$HOME/Library/pnpm" && PATH="$PNPM_HOME:$PATH" && pnpm tsc --noEmit 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - hearth-budget/src/lib/schemas/auth.ts exists
    - hearth-budget/src/lib/schemas/onboarding.ts exists
    - grep "z\.email(" hearth-budget/src/lib/schemas/auth.ts returns matches (v4 syntax)
    - grep "z\.string()\.email\|message:" hearth-budget/src/lib/schemas/auth.ts returns empty (no v3 syntax)
    - grep "INCOME_BRACKETS" hearth-budget/src/lib/schemas/onboarding.ts returns matches
    - grep "\\$15-30k\|\\$70-100k\|\\\$150k+" hearth-budget/src/lib/schemas/onboarding.ts returns matches (all 8 brackets)
    - grep "export type SignUpInput\|export type SignInInput\|export type MagicLinkInput" hearth-budget/src/lib/schemas/auth.ts returns 3 matches
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Schema contracts defined; both files export typed Zod v4 schemas; TypeScript compiles clean</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls hearth-budget/src/components/ui/` → shows all 10 new components plus phase 1 files
2. `ls hearth-budget/src/lib/schemas/` → shows auth.ts and onboarding.ts
3. `grep "z.email(" hearth-budget/src/lib/schemas/auth.ts` → match (Zod v4)
4. `grep "INCOME_BRACKETS" hearth-budget/src/lib/schemas/onboarding.ts` → match
5. `cd hearth-budget && pnpm build` → exits 0
</verification>

<success_criteria>
1. All 10 shadcn components installed and importable from @/components/ui/
2. auth.ts exports signUpSchema, signInSchema, magicLinkSchema with Zod v4 syntax
3. onboarding.ts exports all 3 step schemas and INCOME_BRACKETS const
4. TypeScript compilation passes with no errors
5. pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-onboarding/02-02-SUMMARY.md`
</output>
