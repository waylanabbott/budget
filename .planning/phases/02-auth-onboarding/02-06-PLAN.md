---
phase: 02-auth-onboarding
plan: 06
type: execute
wave: 4
depends_on: [02-04, 02-05]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, HSHD-01, HSHD-02, HSHD-03, HSHD-04, HSHD-05, HSHD-06]

must_haves:
  truths:
    - "User can sign up with email/password and land on /onboarding"
    - "User can sign up with magic link and land on /onboarding after clicking the email link"
    - "User can sign in and land on /app/dashboard"
    - "User can complete all 3 onboarding steps and see their household name in the app shell"
    - "Unauthenticated navigation to /app/dashboard redirects to /login"
    - "Sign out from Settings redirects to /login"
    - "Bottom nav appears on mobile; sidebar appears on desktop"
  artifacts:
    - path: "hearth-budget/proxy.ts"
      provides: "Active Next.js 16 proxy with /app/* auth guard"
    - path: "hearth-budget/src/app/(auth)/login/page.tsx"
      provides: "Tabbed login/signup page"
    - path: "hearth-budget/src/app/(onboarding)/onboarding/page.tsx"
      provides: "3-step onboarding form"
    - path: "hearth-budget/src/app/app/layout.tsx"
      provides: "Protected app shell layout"
  key_links:
    - from: "Sign up flow"
      to: "/onboarding"
      via: "signUpWithPassword redirects to /onboarding after Supabase auth.signUp"
      pattern: "redirect.*onboarding"
    - from: "Onboarding Step 3"
      to: "/app/dashboard"
      via: "createHousehold server action redirects after INSERT"
      pattern: "redirect.*app/dashboard"
    - from: "proxy.ts"
      to: "/login redirect"
      via: "unauthenticated /app/* access blocked at Next.js proxy layer"
      pattern: "pathname.startsWith.*app.*redirect.*login"
---

<objective>
Manual smoke test verification of the complete Phase 2 auth + onboarding + app shell flow.

Purpose: Unit tests cover proxy logic and ZIP lookup, but the auth flow (Supabase network calls, magic link email, session cookies, DB trigger execution) requires a real browser against a live Supabase project. This checkpoint confirms every requirement is satisfied before Phase 2 is closed.

Output: Human-verified confirmation that all 11 Phase 2 requirements pass the smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 implementation:
    - proxy.ts: auth guard for /app/* routes (AUTH-05)
    - /login: tabbed email/password + magic link sign-in (AUTH-01, AUTH-02, AUTH-03)
    - /auth/callback: magic link code exchange (AUTH-02)
    - /onboarding: 3-step household setup (HSHD-01, HSHD-02, HSHD-03, HSHD-04, HSHD-05)
    - /app/* shell: header + bottom nav + sidebar with household name (HSHD-06)
    - /app/settings: sign out button (AUTH-04)
  </what-built>
  <how-to-verify>
    Start the dev server:
    ```bash
    export PNPM_HOME="$HOME/Library/pnpm" && PATH="$PNPM_HOME:$PATH"
    cd /Users/waylansmac/budget/hearth-budget && pnpm dev
    ```
    Open http://localhost:3000 in a browser (Chrome or Safari on mobile for PWA).

    **Test 1 — AUTH-05: Proxy redirect**
    1. While NOT logged in, navigate to http://localhost:3000/app/dashboard
    2. EXPECTED: Immediate redirect to http://localhost:3000/login
    3. PASS: URL changes to /login and login form is visible

    **Test 2 — AUTH-01: Email/password signup**
    1. Go to /login → Sign Up tab
    2. Enter a real email address you can access + a password (8+ chars)
    3. Click "Create Account"
    4. EXPECTED: Redirected to /onboarding
    5. PASS: URL is /onboarding and the 3-step form is visible

    **Test 3 — HSHD-01/02/03: Complete onboarding**
    1. Step 1: Enter a household name (e.g. "Test Household") → click Next
    2. Step 2: Enter a ZIP code (try 84101) → wait 1 second → metro field should auto-populate → click Next
    3. Step 3: Select an income bracket → click "Finish setup"
    4. EXPECTED: Redirected to /app/dashboard
    5. PASS: URL is /app/dashboard and the app shell is visible

    **Test 4 — HSHD-06: App shell with household name**
    1. On /app/dashboard, look at the header
    2. EXPECTED: Your household name appears in the header; your email initial appears as avatar on the right
    3. On mobile viewport (375px): EXPECTED: 5-item bottom nav is visible at the bottom
    4. On desktop viewport (1024px+): EXPECTED: Sidebar with icons + labels is visible on the left
    5. PASS: Both nav patterns render correctly at their respective breakpoints

    **Test 5 — HSHD-04/05: DB triggers (verify in Supabase Studio)**
    1. Open Supabase Studio → Table Editor → household_members
    2. EXPECTED: One row exists for your user_id with role = 'owner'
    3. Open Table Editor → categories
    4. EXPECTED: 20 rows exist with your household_id (seeded by on_household_created_categories trigger)
    5. PASS: Both tables show expected rows

    **Test 6 — AUTH-04: Sign out**
    1. Click Settings in the nav → click "Sign out" button
    2. EXPECTED: Redirected to /login and session is cleared
    3. Try navigating to /app/dashboard again
    4. EXPECTED: Redirected back to /login (proxy still guarding)
    5. PASS: Both redirects work correctly

    **Test 7 — AUTH-03: Session persistence**
    1. Sign in again with your email/password
    2. After landing on /app/dashboard, refresh the page (F5 or Cmd+R)
    3. EXPECTED: Still on /app/dashboard, not redirected to /login
    4. Close the browser tab, reopen, navigate to /app/dashboard
    5. EXPECTED: Still logged in (session cookie persists)
    6. PASS: Session survives refresh and tab close

    **Test 8 — AUTH-02: Magic link (optional — requires email access)**
    1. Sign out first, then go to /login
    2. In the magic link section below the tabs, enter your email → click "Send magic link"
    3. EXPECTED: Toast appears "Check your email for a magic link!" — page stays on /login
    4. Open email client → find the Supabase email → click the link
    5. EXPECTED: Browser redirects to /auth/callback, then to /onboarding or /app/dashboard
    6. PASS: Magic link completes auth and routes correctly

    **Test 9 — HSHD-02: ZIP auto-detection**
    1. Create a second test account (or sign out + sign up again with different email)
    2. In Step 2 of onboarding, type 84101 in the ZIP field
    3. EXPECTED: Metro field auto-fills with "Salt Lake City, UT-ID" without clicking anything
    4. Try 90210 → EXPECTED: "Los Angeles-Long Beach-Anaheim, CA"
    5. Try 99999 → EXPECTED: "National Average"
    6. PASS: Auto-detection works for known ZIPs and falls back for unknown ones

    **Test 10 — Form validation**
    1. On /login → Sign Up tab, submit with empty fields
    2. EXPECTED: Inline validation errors appear (not a full page redirect)
    3. Try a password shorter than 8 chars
    4. EXPECTED: "Password must be at least 8 characters." inline error
    5. PASS: Zod v4 validation errors display inline

    **Run final automated checks:**
    ```bash
    cd /Users/waylansmac/budget/hearth-budget
    export PNPM_HOME="$HOME/Library/pnpm" && PATH="$PNPM_HOME:$PATH"
    pnpm vitest run
    pnpm build
    ```
    Both must exit 0.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 10 tests pass.
    Or describe any failing test with the step number and observed vs expected behavior.
    Example failure: "Test 3 Step 2: ZIP auto-detect not working — metro field stays empty"
  </resume-signal>
</task>

</tasks>

<verification>
Phase 2 complete when:
- All 10 smoke tests pass (human verified above)
- pnpm vitest run exits 0
- pnpm build exits 0
- All 11 requirements confirmed: AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, HSHD-01, HSHD-02, HSHD-03, HSHD-04, HSHD-05, HSHD-06
</verification>

<success_criteria>
1. Unauthenticated /app/* access redirects to /login (AUTH-05)
2. Email/password signup creates user and redirects to /onboarding (AUTH-01)
3. Magic link sends email, stays on /login with toast, callback exchanges code for session (AUTH-02)
4. Signed-in user session persists across refresh and tab close (AUTH-03)
5. Sign out from Settings clears session and redirects to /login (AUTH-04)
6. Onboarding 3 steps collect name, ZIP+metro, income bracket (HSHD-01, HSHD-02, HSHD-03)
7. DB trigger auto-assigns user as 'owner' in household_members (HSHD-04)
8. DB trigger seeds 20 default categories for new household (HSHD-05)
9. App shell header shows household name and user avatar initial (HSHD-06)
10. Bottom nav visible on mobile; sidebar visible on desktop (HSHD-06)
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-onboarding/02-06-SUMMARY.md`

Then update:
- .planning/ROADMAP.md: Mark Phase 2 complete with today's date
- .planning/REQUIREMENTS.md: Mark AUTH-01 through AUTH-05 and HSHD-01 through HSHD-06 as [x]
- .planning/STATE.md: Update current position to Phase 3, update completed_phases count
</output>
