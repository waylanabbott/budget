---
phase: 03-transactions
plan: 06
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified: []
autonomous: false
requirements:
  - TXNS-01
  - TXNS-02
  - TXNS-03
  - TXNS-04
  - TXNS-05
  - TXNS-06
  - TXNS-07
  - TXNS-08
  - ACCT-01
  - ACCT-02
  - ACCT-03
  - CATG-01
  - CATG-02
  - CATG-03

must_haves:
  truths:
    - "All Phase 3 features work end-to-end in a browser"
    - "Transaction entry completable in under 5 seconds on mobile viewport"
    - "TypeScript compiles, build succeeds, tests pass"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 3 features work end-to-end: build succeeds, type-checks pass, and functional smoke test of accounts, categories, transaction entry, and transaction list.

Purpose: Catch integration issues before moving to Phase 4.
Output: Confirmation that Phase 3 is functionally complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transactions/03-04-SUMMARY.md
@.planning/phases/03-transactions/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build, type-check, and fix any integration issues</name>
  <files></files>
  <read_first>
    hearth-budget/src/app/app/layout.tsx,
    hearth-budget/src/app/app/transactions/page.tsx,
    hearth-budget/src/app/app/accounts/page.tsx,
    hearth-budget/src/app/app/settings/categories/page.tsx
  </read_first>
  <action>
Run the following verification commands in sequence:

1. `cd /Users/waylansmac/budget/hearth-budget && npx pnpm exec tsc --noEmit` — fix any TypeScript errors
2. `cd /Users/waylansmac/budget/hearth-budget && npx pnpm build` — fix any build errors
3. `cd /Users/waylansmac/budget/hearth-budget && npx pnpm test` — verify existing tests still pass (32 tests from Phase 2)

If any errors are found, fix them. Common issues to watch for:
- Import path mismatches (especially for new components)
- Missing 'use client' directives on components using hooks
- Type mismatches between Database types and component props
- Sheet component API differences (base-ui render prop vs Radix asChild)
- Zod v4 API issues (remember { error: } not { message: })

After fixing, re-run all three commands to confirm clean state.
  </action>
  <verify>
    <automated>cd /Users/waylansmac/budget/hearth-budget && npx pnpm exec tsc --noEmit && npx pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx pnpm exec tsc --noEmit` exits with code 0
    - `npx pnpm build` exits with code 0
    - `npx pnpm test` shows all tests passing (no regressions from Phase 2)
  </acceptance_criteria>
  <done>TypeScript compiles, Next.js builds, and all existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke test of Phase 3 features</name>
  <files></files>
  <action>
Human verifies all Phase 3 features end-to-end in a running browser. Start dev server with `cd /Users/waylansmac/budget/hearth-budget && npx pnpm dev`, then test accounts CRUD, categories CRUD, transaction entry via FAB + bottom sheet, and transaction list with search/filter/swipe on mobile viewport (375px Chrome DevTools).

What was built:
- Accounts page (/app/accounts) with create, edit, archive
- Categories page (/app/settings/categories) with hierarchical display, create, edit, archive
- Floating "+" button on all /app pages opening transaction entry sheet
- Transaction entry form: amount-first, category picker, merchant autocomplete, date picker
- Transaction list (/app/transactions) with date grouping, infinite scroll, search/filter, swipe actions

Verification steps:
1. Accounts — Navigate to Settings > Accounts. Create "Chase Checking" ($5,000) and "Emergency Fund" ($10,000). Edit one. Verify list shows formatted balances.
2. Categories — Navigate to Settings > Categories. Verify default 20 categories. Create subcategory. Edit color. Archive one.
3. Transaction Entry — Tap "+" button. Verify amount-first with numeric keyboard. Enter $42.50, select category, account, check date=today, add merchant. Submit.
4. Transaction List — Navigate to /app/transactions. Verify date grouping. Search by merchant. Filter by account. Swipe left for edit/delete.
5. Cross-page — Verify "+" button on Dashboard, Budgets, Goals, Settings.
  </action>
  <verify>
    <automated>cd /Users/waylansmac/budget/hearth-budget && npx pnpm exec tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms accounts CRUD works (create, edit, archive)
    - Human confirms categories CRUD works with hierarchy
    - Human confirms floating "+" opens transaction sheet on all /app pages
    - Human confirms transaction saves and appears in list
    - Human confirms search and filter work on transaction list
    - Human confirms swipe-to-reveal actions on mobile viewport
  </acceptance_criteria>
  <done>Human approves all Phase 3 features as working end-to-end. Phase 3 is complete.</done>
</task>

</tasks>

<verification>
- Build and type-check pass
- All Phase 2 tests still pass (no regressions)
- Human confirms end-to-end functionality
</verification>

<success_criteria>
- Zero TypeScript errors
- Zero build errors
- All 32+ tests pass
- Human verifies accounts, categories, transaction entry, and transaction list work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-transactions/03-06-SUMMARY.md`
</output>
